import React from 'react';
import { View, Text } from 'react-native';

const Camera = () => {
  return (
    <View>
      <Text>"You're in the camera"</Text>
    </View>
  );
};

export default Camera;
